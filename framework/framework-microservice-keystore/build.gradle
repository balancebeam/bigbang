description = "keystore module"
archivesBaseName = 'io.anyway.bigbang.framework.keystore'

dependencies {
    api(project(':framework-microservice-bootstrap'))
    api("org.springframework.cloud:spring-cloud-starter-vault-config")
}