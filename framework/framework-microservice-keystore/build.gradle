description = "keystore module"
archivesBaseName = 'io.anyway.bigbang.framework.keystore'

dependencies {
    implementation(project(':framework-microservice-bootstrap'))
    implementation("org.springframework.cloud:spring-cloud-starter-vault-config")
}