description = "The service module of kernal component"
archivesBaseName = 'io.anyway.bigbang.framework.sharding'

dependencies {
    compile(project(":framework:framework-datasource"))
    compile(libraries.shardingjdbc)
}
