description = "executor module"
archivesBaseName = 'io.anyway.bigbang.framework.executor'

dependencies {
    api(project(":framework-microservice-metrics"))
    api("org.springframework.boot:spring-boot-starter-web")
    api("com.alibaba:transmittable-thread-local")
}
