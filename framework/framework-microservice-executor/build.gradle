description = "executor module"
archivesBaseName = 'io.anyway.bigbang.framework.executor'

dependencies {
    compile(project(":framework-microservice-metrics"))
    compile("org.springframework.boot:spring-boot-starter-web")
    compile("com.alibaba:transmittable-thread-local")
}
