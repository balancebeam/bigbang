description = "executor module"
archivesBaseName = 'io.anyway.bigbang.framework.executor'

dependencies {
    implementation(project(":framework-microservice-metrics"))
    implementation("org.springframework.boot:spring-boot-starter-web")
    implementation("com.alibaba:transmittable-thread-local")
}
