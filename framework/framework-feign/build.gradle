description = "The service module of kernal project"
archivesBaseName = 'io.anyway.bigbang.framework.feign'

dependencies {
    compile(project(":framework:framework-core"))
    compile(libraries.spring_cloud_feign)
    compile(libraries.commons_httpclient)
}


