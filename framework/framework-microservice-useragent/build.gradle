description = "useragent module"
archivesBaseName = 'io.anyway.bigbang.framework.useragent'

dependencies {
    api(project(":framework-microservice-header"))
    api(project(":framework-microservice-logging"))
    api("com.alibaba:fastjson")
}
