<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.anyway.bigbang.example.dao.UserMapper" >
  <resultMap id="BaseResultMap" type="io.anyway.bigbang.example.entity.User" >
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="name" property="name" jdbcType="VARCHAR" />
    <result column="password" property="password" jdbcType="VARCHAR" />
    <result column="description" property="description" jdbcType="VARCHAR" />
  </resultMap>

  <sql id="Base_Column_List">
    id, name, password, description
  </sql>

  <select id="getUser" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from bigbang_user
    where id = #{id,jdbcType=BIGINT}
  </select>

  <select id="getUsers" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from bigbang_user
  </select>

  <delete id="deleteUser" parameterType="java.lang.Long">
    delete from bigbang_user
    where id = #{id,jdbcType=BIGINT}
  </delete>

  <insert id="addUser" parameterType="io.anyway.bigbang.example.entity.User" >

    INSERT INTO bigbang_user
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="name != null" >
        id,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="password != null" >
        password,
      </if>
      <if test="description != null" >
        description,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="name != null" >
        #{id,jdbcType=BIGINT},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="password != null" >
        #{password,jdbcType=VARCHAR},
      </if>
      <if test="description != null" >
        #{description,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

</mapper>