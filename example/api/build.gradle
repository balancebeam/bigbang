apply from: "../publish.gradle"
description = "example api project"
archivesBaseName = 'io.anyway.bigbang.example.api'

dependencies {
    compile("org.springframework.cloud:spring-cloud-starter-openfeign")
}
